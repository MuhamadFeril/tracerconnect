<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Development: allow cleartext (HTTP) for all private/local hosts.
        Production:  HTTPS traffic goes through system trust anchors.
        The android:usesCleartextTraffic="true" in the manifest is a
        blanket allow; this config adds fine-grained domain rules so we
        can tighten them later when we move to HTTPS-only production.
    -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Android emulator localhost alias -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>

        <!-- Private network ranges (WiFi / LAN) — covers most dev machines -->
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
    </domain-config>
</network-security-config>
